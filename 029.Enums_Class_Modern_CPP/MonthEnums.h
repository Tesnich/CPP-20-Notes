#ifndef MONTH_ENUMS_H     //if this file has not already be compiled into TU (MONTH_ENUMS_H already defined) do the below
#define MONTH_ENUMS_H     //define MONTH_ENUMS_H

enum class MonthEnums{
    Jan = 1, January = Jan,
    Feb, February = Feb,
    Mar, March = Mar,
    Apr, April = Apr,
    May,
    Jun, June = Jun,
    Jul, July = Jul,
    Aug, August = Aug,
    Sep, September = Sep,
    Oct, October = Oct,
    Nov, November = Nov,
    Dec, December = Dec
};

#endif // MONTH_ENUMS_H
#include <iostream>


int main(int argc, char **argv){

    //setup launch.json file for debugging single file and multiple file builds
    //changed core project template to reflect changes

    //view lecture 27:242 for more info
    //  all said, it was as simple as letting vs code create a launch.json from the debug button,
    //  then adding a new config for lldb, and changing the program name it keys off and adding
    //  the build task as a pre launch item

    return 0;
}